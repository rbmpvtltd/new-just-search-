{
  "name": "ExpoExpressNextjs",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "prebuild": "bun i && turbo run prebuild",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "dbsync": "turbo run dbsync"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.2.20",
  "engines": {
    "bun": "1.2.20"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "react-easy-crop": "^5.5.3"
  }
}
